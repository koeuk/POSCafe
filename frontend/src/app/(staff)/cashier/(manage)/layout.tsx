// Cashier-namespaced management pages (/cashier/categories, /products, /stock,
// /reports) share the exact same padded shell as the admin (manage) routes.
export { default } from "@/app/(staff)/(manage)/layout";
